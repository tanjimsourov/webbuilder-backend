# core/

Core platform primitives: workspaces, sites, memberships, and shared authorization concepts.

- Owns core models/migrations and REST endpoints in `core/urls.py`.
- Intended to be the stable dependency surface for other domain apps.

