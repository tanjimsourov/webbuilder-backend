# shared/search/

Shared search abstraction layer used by domain modules and jobs.

- Wraps the active backend (`builder.search_services`) behind a stable API.
- Provides indexing and document lifecycle helpers for pages, posts, products, and assets.

