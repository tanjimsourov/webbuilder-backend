# forms/

Domain app for form definitions and submission handling.

- Owns forms models/migrations and routes in `forms/urls.py`.
- Public submission endpoints should validate and rate-limit aggressively.

