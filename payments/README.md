# payments/

Domain app for billing and Stripe integration.

- Owns payment/billing models/migrations and routes in `payments/urls.py`.
- Stripe keys and webhook secrets must be provided via environment variables only.

