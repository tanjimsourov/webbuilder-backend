# website/

Website-facing domain APIs for site runtime configuration and deployment metadata.

- Owns APIs for site settings, publish status, domains, robots, and sitemap helpers.
- Reuses `core`, `domains`, and `cms` domain models/services without crossing service boundaries.

